import type { NextPage } from "next";
import Layout from "components/Layout";

const BuilderPage: NextPage = () => (
  <Layout>
    <div>[placeholder for Builder]</div>
  </Layout>
);

export default BuilderPage;
